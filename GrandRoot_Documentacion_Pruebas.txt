================================================================================
  GRANDROOT — DOCUMENTACIÓN DE CÓDIGO (Carpeta: Pruebas)
  Proyecto: Carrito autónomo para análisis inteligente del suelo
  Plataforma principal: ESP32 + Raspberry Pi 5
  Última actualización: 2026-06-05
================================================================================

ÍNDICE
------
  1.  Contador_Pulsos / Contador_Pulsos.ino
  2.  Control_Llanta / Control_Llanta.ino
  3.  Pulsos_Max / Pulsos_Max.ino
  4.  Tabla_DAC_DOWN / Tabla_DAC_DOWN.ino
  5.  Tabla_DAC_UP / Tabla_DAC_UP.ino
  6.  MPU9250 / MPU9250.ino
  7.  Control_PID_Serial / Control_PID_Serial.ino
  8.  Control_PID_WIFI / Control_PID_WIFI.ino
  9.  Prueba_HTML_WIFI / Prueba_HTML_WIFI.ino
 10.  Pruebas_carrito_escala / Carrito_Vision / Carrito_Vision.ino
 11.  Pruebas_carrito_escala / robot_vision_serial.py
 12.  Pruebas_PI_ESP32 / files / leds.ino
 13.  Pruebas_PI_ESP32 / files / server.py
 14.  Prueba_Sensores_Dashboard / esp32_sensores.ino          [NUEVO]
 15.  Prueba_Sensores_Dashboard / serial_bridge.py            [NUEVO]
 16.  Control_PID_RBPI_Flask / grandroot_bridge.py            [NUEVO]
 17.  Control_PID_RBPI_Websockets / grandroot_bridge.py       [NUEVO]
 18.  Unificacion_ESP_Sensores_&_Motores / esp32_sensores.ino [NUEVO]
 19.  Unificacion_ESP_Sensores_&_Motores / Control_PID_WIFI.ino [NUEVO]
 20.  Unificacion_ESP_Sensores_&_Motores / build_dashboard.py [NUEVO]
 21.  GrandRoot-Raspberry / grandroot / app.py                [NUEVO]
 22.  GrandRoot-Raspberry / firmware / ESP32_Sensores_Serial.ino [NUEVO]
 23.  GrandRoot-Raspberry / firmware / Control_PID_Serial.ino [NUEVO]
 24.  DIAGRAMA DE CONEXIONES DE PINES (resumen general)
 25.  RELACIÓN ENTRE ARCHIVOS (qué se conecta con qué)
 26.  EVOLUCIÓN DE LA ARQUITECTURA (resumen de cambios)

================================================================================
1. Contador_Pulsos.ino
================================================================================

PROPÓSITO
  Prueba de integración motores + encoders. Permite controlar los dos motores
  BLDC por comandos de texto desde el Monitor Serial y, al mismo tiempo, lee
  los encoders de cada rueda cada segundo para verificar que los sensores
  funcionan correctamente.

PLATAFORMA
  ESP32

LIBRERÍAS
  Ninguna externa (solo API de Arduino/ESP32).

PINES USADOS
  GPIO 25  — DAC SV_SIGNAL_IZQ  → señal analógica de velocidad motor izquierdo
  GPIO 26  — DAC SV_SIGNAL_DER  → señal analógica de velocidad motor derecho
  GPIO 14  — FR_IZQ             → dirección motor izquierdo (HIGH=adelante)
  GPIO 27  — FR_DER             → dirección motor derecho  (LOW=adelante)
  GPIO 21  — EN_IZQ             → enable motor izquierdo  (OUTPUT+LOW=activo)
  GPIO 33  — EN_DER             → enable motor derecho
  GPIO 32  — ENC_IZQ            → señal encoder izquierdo (interrupción RISING)
  GPIO 34  — ENC_DER            → señal encoder derecho   (interrupción RISING)

HARDWARE CONECTADO
  Drivers BLDC modelo WS55-220 (uno por rueda).

FUNCIONES PRINCIPALES
  contarDer() / contarIzq()
    Rutinas de interrupción (ISR) marcadas con IRAM_ATTR para ejecutarse en
    RAM interna y no perder pulsos. Se disparan en flanco RISING del encoder.

  setup()
    Inicializa Serial a 115200, configura pines de motor como OUTPUT y pines
    de encoder como INPUT_PULLUP. Registra las dos ISR.

  loop()
    - Escucha comandos por Serial:
        "A" → activa motores, FR en HIGH (reversa ambos)
        "B" → desactiva motores, FR en LOW
        "C" → ambos motores activos, DER adelante / IZQ atrás
        "D" → ambos motores activos, DER atrás / IZQ adelante
        "E" → ambos motores activos, FR DER=LOW / IZQ=HIGH (adelante)
        número (0-3300) → interpreta como milivoltios y escribe en DAC
    - Cada 1000 ms imprime pulsos acumulados de cada encoder y los resetea.

FLUJO DE DATOS
  Monitor Serial → ESP32 → DAC/pines FR/EN → Drivers WS55-220 → Motores
  Motores (encoders) → GPIO 32/34 → ISR → contador de pulsos → Serial

--------------------------------------------------------------------------------

================================================================================
2. Control_Llanta.ino
================================================================================

PROPÓSITO
  Prueba básica de control de una sola rueda (o ambas al mismo tiempo) sin
  encoders. Verifica la combinación de pines FR y EN con los drivers WS55-220
  y la conversión de milivoltios a valor DAC.

PLATAFORMA
  ESP32

LIBRERÍAS
  Ninguna externa.

PINES USADOS
  GPIO 25  — SV_SIGNAL_IZQ (DAC velocidad izquierdo)
  GPIO 26  — SV_SIGNAL_DER (DAC velocidad derecho)
  GPIO 21  — EN_IZQ
  GPIO 33  — EN_DER
  GPIO 14  — FR_IZQ  (HIGH=reversa, LOW=adelante para este motor)
  GPIO 27  — FR_DER  (HIGH=reversa, LOW=adelante para este motor)

DIFERENCIAS CON Contador_Pulsos.ino
  - No tiene encoders ni ISR.
  - La inicialización de pines usa INPUT_PULLDOWN antes de cambiar a OUTPUT
    para evitar estados flotantes en el arranque.
  - El caso "C" y "D" tienen la lógica de FR invertida respecto al
    Contador_Pulsos (se detectó un bug de rotación diferencial durante pruebas).

FUNCIONES PRINCIPALES
  setup()
    Configura pines con pulldown primero, luego como OUTPUT.

  loop()
    Idéntica lógica de comandos A/B/C/D/E/número que Contador_Pulsos.ino
    pero sin lectura periódica de encoders.

NOTAS
  Este archivo sirvió como punto de partida para entender el comportamiento
  físico de los motores antes de agregar control PID.

--------------------------------------------------------------------------------

================================================================================
3. Pulsos_Max.ino
================================================================================

PROPÓSITO
  Caracterización de velocidad máxima de los motores. Arranca los motores a
  0 V durante 2 segundos (para que el driver se estabilice) y luego los lleva
  a 3.3 V (DAC=255). Imprime por Serial una tabla CSV con el tiempo y los
  pulsos leídos por encoder cada 200 ms.

PLATAFORMA
  ESP32

LIBRERÍAS
  Ninguna externa.

PINES USADOS
  GPIO 25  — DAC_IZQ
  GPIO 26  — DAC_DER
  GPIO 14  — FR_IZQ  (LOW = adelante)
  GPIO 27  — FR_DER  (LOW = adelante)
  GPIO 21  — EN_IZQ
  GPIO 33  — EN_DER
  GPIO 32  — ENC_IZQ (interrupción)
  GPIO 34  — ENC_DER (interrupción)

CONSTANTES CLAVE
  SAMPLE_MS   = 200    ms entre muestras
  ARRANQUE_MS = 2000   ms en 0 V antes de dar voltaje

FUNCIONES PRINCIPALES
  contarIzq() / contarDer()
    ISR IRAM_ATTR para contar flancos RISING de encoders.

  setup()
    Configura pines, registra ISR, espera 2 s en 0 V,
    luego aplica DAC=255 en ambos canales.
    Imprime encabezado CSV: "t_ms,pulsos_izq,pulsos_der"

  loop()
    Cada SAMPLE_MS lee pulsos de forma atómica (noInterrupts/interrupts),
    imprime la línea CSV y resetea contadores.

SALIDA SERIAL (ejemplo)
  t_ms,pulsos_izq,pulsos_der
  2200,18,17
  2400,22,21
  ...

USO
  Los datos se copian a Excel/Python para calcular la velocidad máxima real
  y calibrar el parámetro PULSOS_MAX usado en los controladores PID.

--------------------------------------------------------------------------------

================================================================================
4. Tabla_DAC_DOWN.ino
================================================================================

PROPÓSITO
  Genera una tabla de caracterización DAC→velocidad en sentido descendente:
  desde DAC=250 hasta DAC=0, bajando de 10 en 10. Para cada valor mide
  pulsos durante 4 segundos y calcula velocidad en m/s.

PLATAFORMA
  ESP32

LIBRERÍAS
  Ninguna externa.

PINES USADOS
  GPIO 25  — SV_SIGNAL_IZQ (DAC)
  GPIO 26  — SV_SIGNAL_DER (DAC)
  GPIO 32  — ENC_DER (interrupción)
  GPIO 34  — ENC_IZQ (interrupción)
  GPIO 33  — EN_DER
  GPIO 21  — EN_IZQ
  GPIO 27  — FR_DER  (LOW = adelante)
  GPIO 14  — FR_IZQ  (HIGH = adelante)

CONSTANTES CLAVE
  DIST_POR_PULSO = 0.0585 m   (circunferencia de rueda / pulsos por vuelta)
  STEP_DAC       = 10         (paso de reducción entre mediciones)
  Min_DAC        = 0
  INTERVALO      = 4000 ms    (duración de cada medición)

FUNCIONES PRINCIPALES
  setup()
    Activa motores en dirección adelante, registra ISR.

  loop()
    Bucle for de DAC=250 a DAC=0:
      - Aplica valor DAC en ambos canales
      - Resetea contadores de pulso
      - Espera 4 segundos midiendo
      - Lee pulsos de forma atómica
      - Calcula: vel = (pulsos × DIST_POR_PULSO) / tiempo
      - Imprime en Serial: DAC, voltaje, pulsos, velocidades
    Al terminar, detiene motores y congela con while(true).

SALIDA SERIAL (ejemplo)
  =================================
  DAC: 250
  Voltaje: 3.235 V
  Pulsos Der: 82 | Pulsos Izq: 80
  Vel Der: 1.197 m/s | Vel Izq: 1.168 m/s

RELACIÓN CON OTROS ARCHIVOS
  Complementario a Tabla_DAC_UP. Juntos generan la curva completa de
  transferencia DAC→velocidad para el modelo del motor.

--------------------------------------------------------------------------------

================================================================================
5. Tabla_DAC_UP.ino
================================================================================

PROPÓSITO
  Igual que Tabla_DAC_DOWN pero en sentido ascendente: desde DAC=0 hasta
  DAC=100 (ajustable), subiendo de 5 en 5. Incluye un reset a 0 V entre
  cada paso para estabilizar el driver.

PLATAFORMA
  ESP32

LIBRERÍAS
  Ninguna externa.

PINES USADOS
  Idénticos a Tabla_DAC_DOWN.

CONSTANTES CLAVE
  DIST_POR_PULSO = 0.0585 m
  STEP_DAC       = 5          (más fino que DOWN para detectar zona muerta)
  MAX_DAC        = 100        (límite superior conservador)
  TIEMPO_MEDICION = 1000 ms
  TIEMPO_RESET    = 300 ms    (tiempo en 0 V entre pasos)

DIFERENCIA PRINCIPAL CON Tabla_DAC_DOWN
  Antes de aplicar cada nuevo valor DAC, primero escribe 0 durante
  TIEMPO_RESET milisegundos. Esto evita que el driver interprete una señal
  ascendente como pico de corriente y entre en protección.

SALIDA SERIAL
  Igual formato que Tabla_DAC_DOWN.

--------------------------------------------------------------------------------

================================================================================
6. MPU9250.ino
================================================================================

PROPÓSITO
  Lee el acelerómetro y giroscopio del sensor MPU9250 vía I2C y los imprime
  por Serial cada 100 ms. No usa librerías externas: implementa toda la
  comunicación I2C manualmente con Wire.h.

PLATAFORMA
  ESP32

LIBRERÍAS
  Wire.h  (incluida en Arduino IDE, sin instalación extra)

CONEXIÓN FÍSICA (I2C)
  MPU9250 VCC  → 3.3 V del ESP32
  MPU9250 GND  → GND
  MPU9250 SCL  → GPIO 22  (I2C SCL por defecto del ESP32)
  MPU9250 SDA  → GPIO 21  (I2C SDA por defecto del ESP32)
  MPU9250 AD0  → GND      → dirección I2C = 0x68
                 (conectar a 3.3 V para usar dirección 0x69)

REGISTROS I2C USADOS
  0x19  REG_SMPLRT_DIV   — divisor de frecuencia de muestreo
  0x1A  REG_CONFIG       — filtro DLPF (vibraciones)
  0x1B  REG_GYRO_CFG     — escala del giroscopio
  0x1C  REG_ACCEL_CFG    — escala del acelerómetro
  0x3B  REG_ACCEL_XOUT   — primer byte acelerómetro (6 bytes seguidos)
  0x43  REG_GYRO_XOUT    — primer byte giroscopio   (6 bytes seguidos)
  0x6B  REG_PWR_MGMT_1   — control de energía (despertar sensor)
  0x75  REG_WHO_AM_I     — identificador chip (0x71 o 0x73)

ESCALAS POR DEFECTO
  Acelerómetro : ±2 g       → 16384 LSB/g
  Giroscopio   : ±250 °/s   →   131 LSB/(°/s)

FUNCIONES PRINCIPALES
  escribirReg(reg, valor) / leerReg(reg) / leerBytes(reg, buf, n)
    Comunicación I2C de bajo nivel con el sensor.

  combinar(hi, lo)
    Combina dos bytes big-endian en un int16_t con signo.

  setup()
    Verifica WHO_AM_I, despierta el sensor, configura escala y filtro.
    Si el sensor no responde, imprime error y detiene la ejecución.

  loop()
    Cada 100 ms lee 6 bytes de acelerómetro y 6 bytes de giroscopio,
    divide por el factor de escala y los imprime en formato tabla.

SALIDA SERIAL (ejemplo)
    Ax(g)    Ay(g)    Az(g)   |   Gx(°/s)  Gy(°/s)  Gz(°/s)
    -------------------------------------------------------
    0.0023   0.0105   1.0012  |     0.152    -0.031     0.008

NOTAS
  - GPIO 21 es compartido con EN_IZQ en otros sketches. No conectar
    el motor y el MPU9250 al mismo tiempo sin cambiar uno de los pines.
  - El magnetómetro del MPU9250 (AK8963) no se lee en este código.
  - En los sketches más recientes de sensores se migró al MPU6050
    (compatible en registros para accel/gyro).

--------------------------------------------------------------------------------

================================================================================
7. Control_PID_Serial.ino
================================================================================

PROPÓSITO
  Controlador PI posicional completo para robot diferencial. Recibe comandos
  por Serial desde una computadora (o Raspberry Pi) y ejecuta un lazo de
  control cerrado usando los encoders de cada rueda. Es la versión base
  de la que derivan las versiones WiFi.

PLATAFORMA
  ESP32

LIBRERÍAS
  Ninguna externa.

PINES USADOS
  GPIO 25  — SV_SIGNAL_IZQ (DAC)
  GPIO 26  — SV_SIGNAL_DER (DAC)
  GPIO 14  — FR_IZQ
  GPIO 27  — FR_DER
  GPIO 21  — EN_IZQ
  GPIO 33  — EN_DER
  GPIO 32  — ENC_IZQ (interrupción RISING)
  GPIO 34  — ENC_DER (interrupción RISING)

PARÁMETROS DEL CONTROLADOR
  kp = 6.0       Ganancia proporcional
  ki = 3.0       Ganancia integral
  SAMPLE_MS = 200        Periodo de muestreo (ms)
  PULSOS_MAX = 22        Pulsos máximos por periodo (velocidad máxima)
  INTEGRAL_MAX = 85.0    Anti-windup: 255/ki
  REF_MIN_GIRO = 12      Referencia mínima en giros para evitar alarma del driver
  DAC_MIN_ARRANQUE = 60  Offset de zona muerta (feedforward)

CINEMÁTICA
  R_RUEDA = 0.1397 m   Radio de rueda
  L_BASE  = 1.12 m     Separación entre ruedas
  VMAX    = 4.0 m/s    Velocidad lineal máxima
  WMAX    = 2.0 rad/s  Velocidad angular máxima

PROTOCOLO SERIAL
  → ENABLE,true          (activar motores)
  → ENABLE,false         (desactivar)
  → MOVE,vx,vy           (vx, vy normalizados [-1,1])
  → STOP                 (detener)
  → KP,val / KI,val / PMAX,val  (ajustar parámetros en caliente)
  ← TEL,pi,pd,ri,rd,ei,ed,daci,dacd   (telemetría cada 200 ms)

RELACIÓN CON OTROS ARCHIVOS
  - grandroot_bridge.py (RPi) se conecta vía USB-Serial a este firmware.
  - robot_vision_serial.py también usa este mismo protocolo.
  - Control_PID_WIFI.ino es la versión equivalente con comunicación WiFi.

--------------------------------------------------------------------------------

================================================================================
8. Control_PID_WIFI.ino
================================================================================

PROPÓSITO
  Versión WiFi del controlador PI. Crea un Access Point propio en el ESP32,
  sirve una página web embebida y se comunica en tiempo real con el navegador
  (o cualquier cliente) mediante WebSocket. La lógica de control es idéntica
  a Control_PID_Serial.ino.

PLATAFORMA
  ESP32

LIBRERÍAS NECESARIAS (instalar en Arduino IDE Library Manager)
  ESPAsyncWebServer  (by lacamera / me-no-dev)
  AsyncTCP           (by dvarrel / me-no-dev)
  WiFi.h             (incluida en ESP32 board package)

ARCHIVOS ADICIONALES
  index_html.h  — contiene la página web embebida como cadena C

RED WIFI CREADA
  SSID     : ESP32-Robot
  Password : robot1234
  IP fija  : 192.168.4.1
  WebSocket: ws://192.168.4.1/ws

PINES USADOS
  Idénticos a Control_PID_Serial.ino.

PROTOCOLO WEBSOCKET
  Idéntico al protocolo Serial más comandos adicionales:
  → ENABLE,true / ENABLE,false
  → MOVE,vx,vy
  → PARAM,nombre,valor    (ajuste en caliente de Kp, Ki, VMAX, etc.)
  ← TEL,pi,pd,ri,rd,ei,ed,daci,dacd   (cada 200 ms)
  ← SENS,{...json...}    (datos de sensores reenviados desde ESP32 SENSORES)
  ← PARAM_OK,nombre,valor

SEGURIDAD
  Si todos los clientes se desconectan, los motores se apagan
  automáticamente en onWsEvent (WS_EVT_DISCONNECT).

RELACIÓN CON OTROS ARCHIVOS
  index_html.h — interfaz gráfica con mapa Leaflet, gráficas Chart.js,
                 panel de telemetría y panel admin (contraseña: grandroot2025).

--------------------------------------------------------------------------------

================================================================================
9. Prueba_HTML_WIFI.ino
================================================================================

PROPÓSITO
  Prueba de servidor web embebido con control de motores vía HTTP (polling),
  sin WebSocket. El celular u otra PC se conecta al WiFi del ESP32 y accede
  a http://192.168.4.1 para controlar el robot.

PLATAFORMA
  ESP32

LIBRERÍAS
  WiFi.h     (incluida)
  WebServer.h (incluida, distinta de ESPAsyncWebServer)
  web_page.h  (archivo .h con el HTML embebido)

RED WIFI CREADA
  SSID     : GrandRoot
  Password : robot1234
  IP fija  : 192.168.4.1

DIFERENCIA PRINCIPAL CON Control_PID_WIFI.ino
  - No hay control PI cerrado. La referencia se mapea directamente a DAC.
  - Comunicación por HTTP polling (no WebSocket).
  - Usa WebServer.h en lugar de ESPAsyncWebServer.

RUTAS HTTP
  GET  /             → sirve la página HTML de control
  POST /api/mover    → recibe JSON {"vx":0.5,"vy":0.0}, aplica cinemática
  GET  /api/tele     → devuelve JSON con pulsos y DAC actuales
  POST /api/enable   → activa/desactiva motores

--------------------------------------------------------------------------------

================================================================================
10. Carrito_Vision.ino
================================================================================

PROPÓSITO
  Firmware para el carrito de prueba a escala (hardware diferente: puente H
  L298N en lugar de drivers WS55-220). Recibe comandos MOVE del script
  robot_vision_serial.py y controla los motores sin encoders usando PWM.

PLATAFORMA
  ESP32

LIBRERÍAS
  Ninguna externa (usa ledcAttach de la API ESP32 Arduino Core 3.x).

PINES USADOS
  GPIO 26  — EN_A    (PWM motor izquierdo — canal LEDC)
  GPIO 27  — IN1     (dirección motor izquierdo)
  GPIO 18  — IN2
  GPIO 19  — IN3     (dirección motor derecho)
  GPIO 14  — IN4
  GPIO 25  — EN_B    (PWM motor derecho — canal LEDC)

HARDWARE CONECTADO
  Puente H L298N (diferente a los otros sketches que usan WS55-220).

PARÁMETROS
  PWM_FREQ       = 1000 Hz
  PWM_BITS       = 8        (valores 0-255)
  VELOCIDAD_BASE = 180      (duty cycle fijo cuando avanza)
  VX_UMBRAL      = 0.05     (mínimo vx para moverse)
  WATCHDOG_MS    = 600      (ms sin comando MOVE → detener motores)

PROTOCOLO SERIAL
  Idéntico a Control_PID_Serial.ino.
  Responde con eco TEL para compatibilidad con robot_vision_serial.py.

RELACIÓN CON OTROS ARCHIVOS
  robot_vision_serial.py — script Python que alimenta los comandos MOVE
  basándose en la detección del modelo U-Net.

--------------------------------------------------------------------------------

================================================================================
11. robot_vision_serial.py
================================================================================

PROPÓSITO
  Script Python que combina visión por computadora con control de motores.
  Captura frames de una cámara, los procesa con un modelo U-Net entrenado
  (modelo_final.h5), detecta la dirección del surco o camino, y envía
  comandos MOVE al ESP32 por Serial.

PLATAFORMA
  PC o Laptop con Python 3.x

LIBRERÍAS
  cv2, numpy, tensorflow, serial (pyserial), threading, time, sys, os

ARCHIVOS DE ENTRADA
  modelo_final.h5   — modelo U-Net entrenado para segmentación de camino (~23 MB)

FLUJO DE PROCESAMIENTO
  1. Captura frame de cámara → redimensiona 128×128 → normaliza (0-1)
  2. Inferencia U-Net → máscara binaria del camino
  3. Calcula centroide de la máscara
  4. Determina ángulo de desviación respecto al centro del frame
  5. Si confianza < CONF_MIN_ACCION (0.30) → MOVE,0,0 (detener)
  6. Si ángulo < ANGLE_UMBRAL (3.0°) → ir recto: MOVE,vx,0
  7. Si ángulo ≥ ANGLE_UMBRAL → girar: MOVE,vx,±vy
  8. Envía comando por Serial al ESP32

CONTROLES DE TECLADO
  Q/ESC → salir | SPACE → pausar | E → toggle motores | S → screenshot
  + / − → subir/bajar REF_PULSOS en tiempo real

RELACIÓN CON OTROS ARCHIVOS
  Carrito_Vision.ino  — firmware del ESP32 que recibe los comandos
  modelo_final.h5     — modelo de segmentación

--------------------------------------------------------------------------------

================================================================================
12. Pruebas_PI_ESP32 / files / leds.ino
================================================================================

PROPÓSITO
  Prueba mínima de comunicación entre ESP32 y Raspberry Pi. El ESP32 recibe
  comandos de un carácter por Serial y enciende un LED de color correspondiente.

PLATAFORMA
  ESP32

PINES USADOS
  GPIO 2  — LED rojo
  GPIO 4  — LED azul

COMANDOS SERIAL (a 9600 baud)
  'R' → enciende LED rojo, apaga azul
  'B' → enciende LED azul, apaga rojo

RELACIÓN CON OTROS ARCHIVOS
  server.py — servidor Flask en la RPi que envía los comandos al ESP32.

--------------------------------------------------------------------------------

================================================================================
13. Pruebas_PI_ESP32 / files / server.py
================================================================================

PROPÓSITO
  Servidor web Flask que corre en la Raspberry Pi y sirve como intermediario
  entre un navegador web y el ESP32. Es la prueba de concepto del puente
  HTTP ↔ Serial que luego se amplió en grandroot_bridge.py.

PLATAFORMA
  Raspberry Pi (Python 3.x)

LIBRERÍAS
  flask, serial (pyserial), time

CONFIGURACIÓN
  SERIAL_PORT = '/dev/ttyUSB0'
  BAUD_RATE   = 9600

RUTAS HTTP
  GET  /        → sirve leds.html (interfaz de control)
  POST /led     → recibe JSON {"color":"rojo"} o {"color":"azul"},
                  envía 'R\n' o 'B\n' por Serial al ESP32

ARRANQUE
  python3 server.py  →  Escucha en 0.0.0.0:8080

EVOLUCIÓN
  Este archivo es la versión simplificada. grandroot_bridge.py (Flask y
  WebSockets) son sus sucesores con soporte completo de motores.

--------------------------------------------------------------------------------

================================================================================
14. Prueba_Sensores_Dashboard / esp32_sensores.ino  [NUEVO]
================================================================================

PROPÓSITO
  Primera versión del firmware de sensores combinado GPS + IMU. Lee el módulo
  GPS NEO-6M/8M por Serial2 (TinyGPS++) y el IMU MPU6050 por I2C, y publica
  un paquete JSON por USB-Serial cada 200 ms. Sirve de fuente de datos para
  el dashboard de la Prueba_Sensores_Dashboard.

PLATAFORMA
  ESP32 (ESP32 SENSORES — unidad separada del ESP32 de motores)

LIBRERÍAS
  Wire.h       (incluida)
  TinyGPS++    (instalar en Library Manager)

PINES USADOS
  GPIO 16  — GPS_RX (RXD2 ← GPS TXD)
  GPIO 17  — GPS_TX (TXD2 → GPS RXD, no se usa en recepción)
  GPIO 21  — IMU SDA (I2C)
  GPIO 22  — IMU SCL (I2C)

CONFIGURACIÓN CLAVE
  GPS_BAUDRATE   = 9600
  IMU_ADDR       = 0x68   (AD0=GND)
  ACCEL_SCALE    = 16384.0 LSB/g   (rango ±2 g)
  GYRO_SCALE     = 131.0  LSB/(°/s)
  INTERVALO_MS   = 200 ms
  LAT_REF / LON_REF — punto de referencia para coordenadas locales X/Y

SALIDA SERIAL (115200 baud)
  Prefijo SENS, seguido de JSON:
  SENS,{"lat":28.7385,"lon":-106.1217,"x":0.0,"y":0.0,"sats":7,
        "ax":0.01,"ay":-0.02,"az":0.98,"gx":0.1,"gy":-0.3,"gz":0.05,
        "gps_ok":true,"imu_ok":true,"ts":12345}

  Campos:
    lat, lon    — coordenadas GPS (grados decimales)
    x, y        — posición local en metros desde LAT_REF / LON_REF
    sats        — satélites visibles
    ax,ay,az    — aceleración en g
    gx,gy,gz    — velocidad angular en °/s
    gps_ok      — true si el GPS tiene fix válido
    imu_ok      — true si el IMU respondió correctamente
    ts          — millis() del ESP32 (para detectar paquetes perdidos)

NOTAS
  - El GPS necesita cielo abierto; el primer fix puede tardar 1-2 minutos.
  - Registros de IMU son los mismos que MPU9250.ino (compatibles).
  - GPIO 21 comparte función con EN_IZQ en sketches de motor: no combinar.

RELACIÓN CON OTROS ARCHIVOS
  serial_bridge.py — lee la salida Serial de este firmware y la reenvía
                     al dashboard HTML mediante Socket.IO.

--------------------------------------------------------------------------------

================================================================================
15. Prueba_Sensores_Dashboard / serial_bridge.py  [NUEVO]
================================================================================

PROPÓSITO
  Puente Flask + Socket.IO entre el ESP32 SENSORES y el dashboard web. Lee el
  JSON del ESP32 por USB-Serial y lo emite en tiempo real al navegador vía
  Socket.IO. Es la evolución de server.py orientada a datos de sensores.

PLATAFORMA
  PC, laptop o Raspberry Pi con Python 3.x

LIBRERÍAS
  flask, flask-socketio, pyserial

INSTALACIÓN
  pip install flask flask-socketio pyserial

USO
  python serial_bridge.py                        # autodetecta puerto
  python serial_bridge.py --port COM3            # Windows
  python serial_bridge.py --port /dev/ttyUSB0   # Linux
  Luego abrir: http://localhost:5000

CONFIGURACIÓN
  BAUDRATE      = 115200
  HOST          = "0.0.0.0"
  PORT_HTTP     = 5000
  DASHBOARD_FILE = "grandroot_dashboard.html"

AUTODETECCIÓN DE PUERTO
  Prueba en orden: /dev/ttyUSB*, /dev/ttyACM*, /dev/cu.usbserial*,
  /dev/cu.SLAB*, /dev/cu.wchusbserial*, COM3-COM19 (Windows).

FLUJO DE DATOS
  ESP32 Sensores → Serial USB → serial_bridge.py → Socket.IO → Navegador
                                (grandroot_dashboard.html)

EVENTO SOCKET.IO EMITIDO
  'sensor_data'  — emite el JSON del sensor tal como llega del ESP32.

RELACIÓN CON OTROS ARCHIVOS
  esp32_sensores.ino     — firmware del ESP32 que produce los datos
  grandroot_dashboard.html — interfaz de dashboard en el navegador

--------------------------------------------------------------------------------

================================================================================
16. Control_PID_RBPI_Flask / grandroot_bridge.py  [NUEVO]
================================================================================

PROPÓSITO
  Puente Flask (HTTP polling) entre la Raspberry Pi 5 y el ESP32 de motores.
  Sirve el panel de control HTML y traduce peticiones HTTP en comandos Serial
  hacia el ESP32. La telemetría del ESP32 se lee continuamente en un hilo
  y se expone por GET /telemetria.

PLATAFORMA
  Raspberry Pi 5 (Python 3.x)

LIBRERÍAS
  flask, serial (pyserial), threading, time, os

CONFIGURACIÓN
  SERIAL_PORT = '/dev/ttyESP32'   (symlink udev estable)
  BAUD_RATE   = 115200
  HTML_FILE   = grandroot.html (mismo directorio)

ENDPOINTS HTTP
  GET  /            → sirve grandroot.html
  GET  /telemetria  → último TEL como JSON {pi,pd,ri,rd,ei,ed,daci,dacd,ts}
  POST /move        → {vx, vy}         → escribe "MOVE,vx,vy"
  POST /enable      → {on: true|false} → escribe "ENABLE,true|false"
  POST /param       → {nombre, valor}  → escribe "PARAM,nombre,valor"
  GET  /status      → estado de la conexión serial

COMPONENTES INTERNOS
  Hilo lector Serial
    Corre en segundo plano. Lee líneas del ESP32 continuamente,
    parsea las que comienzan con "TEL," y actualiza _telemetria
    protegido por _tel_lock.

  serial_write(cmd)
    Thread-safe: usa serial_lock para serializar escrituras.

DIFERENCIA CON LA VERSIÓN WEBSOCKETS
  Esta versión usa HTTP polling (el navegador pide /telemetria
  periódicamente). Más simple pero con mayor latencia que WebSocket.

ARCHIVOS DE CONFIGURACIÓN TXT (misma carpeta)
  grandroot_01_hotspot.txt     — crear hotspot WiFi en la RPi
  grandroot_02_systemd.txt     — registrar bridge como servicio systemd
  grandroot_03_switch_modos.txt — alternar modo hotspot/cliente WiFi
  grandroot_04_mantenimiento.txt — diagnóstico y mantenimiento

RELACIÓN CON OTROS ARCHIVOS
  Control_PID_RBPI.ino — firmware del ESP32 que se comunica con este bridge
  grandroot.html       — interfaz web servida al navegador

--------------------------------------------------------------------------------

================================================================================
17. Control_PID_RBPI_Websockets / grandroot_bridge.py  [NUEVO]
================================================================================

PROPÓSITO
  Versión WebSocket del puente RPi ↔ ESP32. Usa asyncio + websockets para
  comunicación bidireccional de baja latencia entre el navegador y el ESP32.
  Reemplaza el HTTP polling de la versión Flask.

PLATAFORMA
  Raspberry Pi 5 (Python 3.x)

LIBRERÍAS
  pyserial, websockets, asyncio, http.server, threading, argparse, logging

INSTALACIÓN
  pip install pyserial websockets

USO
  python3 grandroot_bridge.py [--port /dev/ttyUSB0] [--baud 115200]
                               [--http-port 8080] [--ws-port 8765]

ARQUITECTURA
  Navegador ←── WS:8765 ──→ RPi bridge ←── USB-Serial ──→ ESP32
  Navegador ←── HTTP:8080 ─→ RPi (sirve index.html)

COMPONENTES INTERNOS
  State (clase)
    Contiene: serial_conn, ws_clients, serial_lock, loop.
    Compartida entre hilos y corutinas.

  serial_write(line)
    Thread-safe con serial_lock. Evita condiciones de carrera
    al escribir desde múltiples clientes WebSocket simultáneos.

  serial_reader_thread()
    Hilo bloqueante que lee líneas del ESP32. Por cada línea,
    llama asyncio.run_coroutine_threadsafe() para broadcast
    a todos los clientes WebSocket.

  ws_handler(websocket)
    Corutina asíncrona. Registra el cliente, recibe mensajes y los
    envía al ESP32 por Serial. Al desconectarse, limpia el registro.

  http_server_thread(port)
    Hilo que sirve index.html vía SimpleHTTPRequestHandler.

  main()
    Abre Serial, lanza hilos, inicia servidor WebSocket,
    captura SIGINT/SIGTERM para cierre limpio.

PROTOCOLO (idéntico al del firmware)
  → ENABLE,true / ENABLE,false
  → MOVE,vx,vy
  ← TEL,pi,pd,ri,rd,ei,ed,daci,dacd

RELACIÓN CON OTROS ARCHIVOS
  Control_PID_RBPI.ino — firmware del ESP32
  index.html           — interfaz web con joystick y telemetría

--------------------------------------------------------------------------------

================================================================================
18. Unificacion_ESP_Sensores_&_Motores / esp32_sensores.ino  [NUEVO]
================================================================================

PROPÓSITO
  Versión avanzada del firmware de sensores. Introduce FreeRTOS y ESP-NOW
  para eliminar el cable UART entre los dos ESP32. El ESP32 SENSORES envía
  los datos de GPS + IMU inalámbricamente al ESP32 de motores (PID), que
  los reenvía al navegador por WebSocket junto con la telemetría de motores.

PLATAFORMA
  ESP32 SENSORES (unidad dedicada a sensores)

LIBRERÍAS
  Wire.h, TinyGPS++, WiFi.h, esp_now.h, esp_mac.h
  (ESP-NOW y FreeRTOS incluidos en el core ESP32 de Arduino)

PINES USADOS
  Idénticos al firmware de Prueba_Sensores_Dashboard.
  GPIO 16 (GPS RX), GPIO 17 (GPS TX), GPIO 21 (SDA), GPIO 22 (SCL)

ARQUITECTURA INTERNA (FreeRTOS)
  Tarea Core 0 — IMU (MPU6050 por I2C)
    Si el sensor se cuelga, solo esta tarea se bloquea; GPS y ESP-NOW
    siguen operando en Core 1.

  Tarea Core 1 — GPS + envío ESP-NOW
    Lee tramas NMEA, actualiza posición y envía el paquete consolidado.

  Semáforo (SemaphoreHandle_t)
    Protege los datos compartidos entre las dos tareas.

CONFIGURACIÓN ESP-NOW
  1. Sube este sketch al ESP32 SENSORES; abre Monitor Serie.
  2. Copia la MAC que aparece: "[ESP-NOW] Mi MAC: XX:XX:XX:XX:XX:XX"
  3. Pégala en Control_PID_WIFI_espnow.ino como PEER_MAC.
  4. Sube ese sketch al ESP32 PID.

CABLE UART ELIMINADO
  Esta versión no requiere ningún cable entre los ESP32. La comunicación
  es 100% inalámbrica por ESP-NOW (WiFi 2.4 GHz, latencia ~1 ms).

RELACIÓN CON OTROS ARCHIVOS
  Control_PID_WIFI.ino (versión espnow) — receptor de los datos en el ESP32 PID.

--------------------------------------------------------------------------------

================================================================================
19. Unificacion_ESP_Sensores_&_Motores / Control_PID_WIFI.ino  [NUEVO]
================================================================================

PROPÓSITO
  Versión unificada del controlador PID + WiFi que integra la recepción de
  datos del ESP32 SENSORES (por ESP-NOW o UART Serial1). Sirve el dashboard
  completo embebido en PROGMEM (~424 KB) con mapa Leaflet y gráficas Chart.js.
  Sistema 100% offline: no requiere Raspberry Pi ni internet en operación.

PLATAFORMA
  ESP32 PID (unidad de motores)

LIBRERÍAS
  ESPAsyncWebServer, AsyncTCP, WiFi.h
  (+ esp_now.h si se usa la versión ESP-NOW)

ARCHIVOS ADICIONALES
  index_html.h  — dashboard completo embebido (Leaflet + Chart.js en PROGMEM)

RED WIFI
  SSID     : ESP32-Robot
  Password : robot1234
  IP       : 192.168.4.1

PROTOCOLO WEBSOCKET EXTENDIDO
  Nuevos mensajes respecto a versión anterior:
  → PARAM,nombre,valor    — ajustar Kp, Ki, VMAX, WMAX, etc.
  ← SENS,{...json...}    — reenvía datos del ESP32 SENSORES al navegador
  ← PARAM_OK,nombre,valor — confirma el cambio de parámetro

PANEL ADMIN (contraseña: grandroot2025)
  Permite ajustar en tiempo real: Kp, Ki, Pulsos Max, VMAX, WMAX,
  Ref Min Giro, DAC Min Arranque.

MAPA (Leaflet)
  Tres capas seleccionables sin API key:
  - Satélite — ArcGIS World Imagery
  - Calles   — CartoDB Voyager
  - Topo     — OpenTopoMap
  Tiles cacheables offline en IndexedDB del navegador.

RELACIÓN CON OTROS ARCHIVOS
  esp32_sensores.ino  — fuente de datos de GPS/IMU
  index_html.h        — dashboard generado por build_dashboard.py

--------------------------------------------------------------------------------

================================================================================
20. Unificacion_ESP_Sensores_&_Motores / build_dashboard.py  [NUEVO]
================================================================================

PROPÓSITO
  Script de build que toma el dashboard HTML fuente más las librerías
  JavaScript (Leaflet, Chart.js) y genera dos artefactos listos para producción:
    1. grandroot_dashboard_offline.html — standalone para desarrollo en PC
    2. index_html.h                     — cabecera C para embeber en el ESP32

PLATAFORMA
  PC con Python 3.x

USO
  python3 build_dashboard.py

ARCHIVOS DE ENTRADA
  grandroot_dashboard.html   — HTML fuente (editar aquí los cambios)
  leaflet.js / leaflet.css   — librería de mapas
  chart.js                   — librería de gráficas

ARCHIVOS DE SALIDA
  grandroot_dashboard_offline.html  — para probar en navegador con internet
  index_html.h                      — ~424 KB; se incluye en el sketch Arduino

WORKFLOW DE DESARROLLO
  1. Edita grandroot_dashboard.html
  2. Ejecuta python3 build_dashboard.py
  3. Prueba en PC abriendo grandroot_dashboard_offline.html
  4. Sube Control_PID_WIFI.ino al ESP32 (incluye index_html.h)

--------------------------------------------------------------------------------

================================================================================
21. GrandRoot-Raspberry / grandroot / app.py  [NUEVO]
================================================================================

PROPÓSITO
  Servidor Flask + Socket.IO centralizado en la Raspberry Pi. Maneja
  simultáneamente dos conexiones Serial USB: una al ESP32 de motores y otra
  al ESP32 de sensores. Retransmite todos los datos al navegador por Socket.IO.
  Es la arquitectura de producción más completa del proyecto.

PLATAFORMA
  Raspberry Pi 5 (Python 3.x)

LIBRERÍAS
  flask, flask-socketio, pyserial, eventlet

INSTALACIÓN
  pip install flask flask-socketio pyserial eventlet

PUERTOS SERIALES
  SERIAL_MOTORES  = "/dev/ttyESP32"     (symlink udev del ESP32 de motores)
  SERIAL_SENSORES = "/dev/ttySensores"  (symlink udev del ESP32 de sensores)
  SERIAL_BAUD     = 115200

ARRANQUE
  cd grandroot && python app.py
  Navegar a http://<ip-de-la-raspberry>:5000

ALIAS DE PUERTOS USB (udev — para nombres estables)
  Crear /etc/udev/rules.d/99-grandroot.rules:
    SUBSYSTEM=="tty", ATTRS{idVendor}=="10c4", ATTRS{idProduct}=="ea60",
      ATTRS{serial}=="<SERIAL_MOTORES>", SYMLINK+="ttyESP32"
    SUBSYSTEM=="tty", ATTRS{idVendor}=="10c4", ATTRS{idProduct}=="ea60",
      ATTRS{serial}=="<SERIAL_SENSORES>", SYMLINK+="ttySensores"

  idVendor 10c4 / idProduct ea60 = chip CP2102. CH340: usar 1a86/7523.
  Recarga: sudo udevadm control --reload-rules && sudo udevadm trigger

ARQUITECTURA INTERNA
  Hilo lector ESP32 Motores
    Lee líneas "TEL,..." y emite evento socket 'tel' al navegador.
    Reintenta conexión automáticamente si el puerto se desconecta.

  Hilo lector ESP32 Sensores
    Lee líneas "SENS,{...}" y emite evento socket 'sens' al navegador.
    Reintenta conexión automáticamente.

  Estado global (state_lock)
    Diccionarios 'tel', 'sens' y 'status' protegidos por threading.Lock.

EVENTOS SOCKET.IO
  Cliente → servidor:
    'move'   — {vx, vy}
    'enable' — {on: true|false}
    'stop'   — detener motores
    'param'  — {nombre, valor}

  Servidor → cliente:
    'tel'    — telemetría de motores (cada vez que llega del ESP32)
    'sens'   — datos de sensores GPS/IMU
    'status' — estado de conexión de cada puerto Serial

AUTO-ARRANQUE (systemd)
  /etc/systemd/system/grandroot.service:
    ExecStart=/usr/bin/python3 /home/pi/grandroot/app.py
    WorkingDirectory=/home/pi/grandroot
    Restart=always
  sudo systemctl enable grandroot && sudo systemctl start grandroot

RELACIÓN CON OTROS ARCHIVOS
  firmware/ESP32_Motores/Control_PID_Serial.ino  — firmware motores
  firmware/ESP32_Sensores/ESP32_Sensores_Serial.ino — firmware sensores
  templates/index.html  — dashboard servido al navegador (idéntico al original)

--------------------------------------------------------------------------------

================================================================================
22. GrandRoot-Raspberry / firmware / ESP32_Sensores_Serial.ino  [NUEVO]
================================================================================

PROPÓSITO
  Versión final del firmware de sensores para la arquitectura Raspberry Pi.
  Elimina WiFi y ESP-NOW; se comunica únicamente por USB-Serial con la RPi.
  Usa FreeRTOS (dos tareas en núcleos separados) para GPS e IMU.

PLATAFORMA
  ESP32 SENSORES

LIBRERÍAS
  Wire.h, TinyGPS++

DIFERENCIAS RESPECTO A VERSIONES ANTERIORES
  - WiFi y ESP-NOW eliminados completamente.
  - FreeRTOS: IMU en Core 0, GPS + publicación en Core 1.
  - La Raspberry Pi (app.py) reenvía los datos al dashboard por Socket.IO.

SALIDA SERIAL
  Prefijo "SENS," + JSON — mismo formato que la versión anterior:
  SENS,{"lat":...,"lon":...,"x":...,"y":...,"sats":...,
        "ax":...,"ay":...,"az":...,"gx":...,"gy":...,"gz":...,
        "gps_ok":...,"imu_ok":...,"ts":...}

CONEXIÓN
  ESP32 USB → /dev/ttySensores en la Raspberry Pi (alias udev)

PINES USADOS
  GPIO 16 (GPS RX), GPIO 17 (GPS TX), GPIO 21 (SDA), GPIO 22 (SCL)
  (Sin cambios respecto a versiones anteriores)

RELACIÓN CON OTROS ARCHIVOS
  app.py — lee la salida de este firmware y la emite por Socket.IO

--------------------------------------------------------------------------------

================================================================================
23. GrandRoot-Raspberry / firmware / ESP32_Motores / Control_PID_Serial.ino  [NUEVO]
================================================================================

PROPÓSITO
  Versión del controlador PID para la arquitectura Raspberry Pi. Sin WiFi;
  se comunica únicamente por USB-Serial con la RPi. Protocolo extendido
  respecto a la versión base.

PLATAFORMA
  ESP32 MOTORES

LIBRERÍAS
  Ninguna externa.

PROTOCOLO SERIAL EXTENDIDO
  Entrada (← Raspberry):
    MOVE,vx,vy    — mover
    STOP          — detener
    EN,1 / EN,0   — activar/desactivar motores
    KP,val        — ajustar Kp en caliente
    KI,val        — ajustar Ki en caliente
    PMAX,val      — ajustar PULSOS_MAX en caliente

  Salida (→ Raspberry):
    TEL,medida_i,medida_d,ref_i,ref_d,error_i,error_d,dac_i,dac_d
    ENABLE,true/false
    PARAM_OK,nombre,valor

CONEXIÓN
  ESP32 USB → /dev/ttyESP32 en la Raspberry Pi (alias udev)

PINES USADOS
  Idénticos a las versiones anteriores del PID.

RELACIÓN CON OTROS ARCHIVOS
  app.py — lee TEL y envía MOVE/STOP/EN/KP/KI/PMAX

--------------------------------------------------------------------------------

================================================================================
24. DIAGRAMA DE CONEXIONES DE PINES (Resumen General)
================================================================================

ESP32 — Drivers BLDC WS55-220 (sketches principales)
  GPIO 25 → SV_SIGNAL_IZQ   DAC de velocidad motor izquierdo
  GPIO 26 → SV_SIGNAL_DER   DAC de velocidad motor derecho
  GPIO 14 → FR_IZQ          Dirección motor izq (HIGH=adelante*)
  GPIO 27 → FR_DER          Dirección motor der (LOW=adelante*)
  GPIO 21 → EN_IZQ          Enable motor izq (OUTPUT+LOW=activo)
  GPIO 33 → EN_DER          Enable motor der (OUTPUT+LOW=activo)
  GPIO 32 → ENC_IZQ         Encoder izquierdo (pull-up externo 10kΩ)
  GPIO 34 → ENC_DER         Encoder derecho   (pull-up externo 10kΩ)

  * La inversión en FR_DER (LOW=adelante) es intencional para compensar
    el montaje espejo de los motores en el chasis.

ESP32 — Puente H L298N (Carrito_Vision)
  GPIO 26 → EN_A   PWM motor izquierdo
  GPIO 27 → IN1    Dirección motor izquierdo
  GPIO 18 → IN2
  GPIO 19 → IN3    Dirección motor derecho
  GPIO 14 → IN4
  GPIO 25 → EN_B   PWM motor derecho

ESP32 SENSORES — GPS NEO-6M/8M + IMU MPU6050 (todos los sketches de sensores)
  GPIO 16 → GPS_RX  (Serial2 RXD2 ← GPS TXD)
  GPIO 17 → GPS_TX  (Serial2 TXD2 → GPS RXD, solo TX)
  GPIO 21 → SDA I2C (MPU6050)
  GPIO 22 → SCL I2C (MPU6050)
  3.3 V   → VCC MPU6050 / VIN GPS (5 V o 3.3 V según módulo)
  GND     → GND + AD0 del MPU6050 (dirección I2C = 0x68)

ESP32 — MPU9250 (MPU9250.ino — prueba original)
  GPIO 22 → SCL
  GPIO 21 → SDA   ← CONFLICTO con EN_IZQ en sketches de motor
  3.3 V   → VCC
  GND     → GND + AD0 (dirección 0x68)

ESP32 — LEDs de prueba (leds.ino)
  GPIO 2  → LED rojo
  GPIO 4  → LED azul

ESP32 SENSORES → ESP32 PID (versión UART — Unificacion, primera versión)
  GPIO 4 (TX1) del SENSORES → GPIO 16 (RX2) del PID
  GND común obligatorio

ESP32 SENSORES → ESP32 PID (versión ESP-NOW — sin cable)
  Comunicación inalámbrica WiFi 2.4 GHz
  Configurar PEER_MAC en el receptor con la MAC del SENSORES

Raspberry Pi 5 — ESP32 Motores
  USB-A (RPi) ← USB micro/C (ESP32)  →  /dev/ttyESP32  @ 115200 baud

Raspberry Pi 5 — ESP32 Sensores
  USB-A (RPi) ← USB micro/C (ESP32)  →  /dev/ttySensores  @ 115200 baud

================================================================================
25. RELACIÓN ENTRE ARCHIVOS (Qué se conecta con qué)
================================================================================

SISTEMA COMPLETO CON RASPBERRY PI (arquitectura final):
  index.html (navegador)
      ↕ Socket.IO :5000
  app.py  (Raspberry Pi)
      ↕ /dev/ttyESP32 @ 115200
  Control_PID_Serial.ino  (ESP32 Motores)
      ↕ DAC/GPIO
  Drivers WS55-220 → Motores BLDC
      ↕ /dev/ttySensores @ 115200
  ESP32_Sensores_Serial.ino  (ESP32 Sensores)
      ↑ GPS NEO-6M/8M + MPU6050

SISTEMA UNIFICADO STANDALONE (sin RPi, 100% offline):
  Navegador (celular/PC)
      ↕ WebSocket ws://192.168.4.1/ws
  Control_PID_WIFI.ino + index_html.h  (ESP32 PID — AP)
      ↕ ESP-NOW / UART Serial1
  esp32_sensores.ino  (ESP32 SENSORES)
      ↑ GPS NEO-6M/8M + MPU6050
      ↕ DAC/GPIO
  Drivers WS55-220 → Motores BLDC

SISTEMA SENSORES + DASHBOARD (prueba intermedia):
  Navegador → grandroot_dashboard.html
      ↕ Socket.IO :5000
  serial_bridge.py  (PC/RPi)
      ↕ USB-Serial 115200
  esp32_sensores.ino (primera versión UART)
      ↑ GPS NEO-6M/8M + MPU6050

SISTEMA CON RASPBERRY PI — VERSIÓN FLASK (HTTP polling):
  Navegador → grandroot.html
      ↕ HTTP polling :5000
  Control_PID_RBPI_Flask/grandroot_bridge.py  (Raspberry Pi)
      ↕ /dev/ttyESP32 @ 115200
  Control_PID_RBPI.ino  (ESP32)
      ↕ DAC/GPIO
  Drivers WS55-220 → Motores BLDC

SISTEMA CON RASPBERRY PI — VERSIÓN WEBSOCKETS:
  Navegador (joystick) → index.html
      ↕ WebSocket :8765
  Control_PID_RBPI_Websockets/grandroot_bridge.py  (Raspberry Pi)
      ↕ USB-Serial 115200
  Control_PID_RBPI.ino  (ESP32)
      ↕ DAC/GPIO
  Drivers WS55-220 → Motores BLDC

SISTEMA AUTÓNOMO POR VISIÓN:
  modelo_final.h5
      ↓
  robot_vision_serial.py  (PC/Laptop)
      ↕ USB-Serial 115200
  Carrito_Vision.ino  (ESP32)
      ↕ PWM/GPIO
  Puente H L298N → Motores DC

SISTEMA WIFI SIMPLE (HTTP polling, sin RPi):
  Navegador (celular)
      ↕ HTTP http://192.168.4.1
  Prueba_HTML_WIFI.ino + web_page.h  (ESP32 AP)
      ↕ DAC/GPIO
  Drivers WS55-220 → Motores BLDC

PRUEBA RPi-ESP32 BÁSICA (origen del puente):
  Navegador → leds.html
      ↕ HTTP :8080
  server.py  (Raspberry Pi)
      ↕ USB-Serial 9600
  leds.ino  (ESP32)
      ↕ GPIO 2/4
  LEDs rojo/azul

PRUEBAS DE CARACTERIZACIÓN (standalone ESP32):
  Tabla_DAC_UP.ino    → curva velocidad vs DAC (ascendente)
  Tabla_DAC_DOWN.ino  → curva velocidad vs DAC (descendente)
  Pulsos_Max.ino      → mide velocidad máxima a 3.3 V
  Contador_Pulsos.ino → verifica encoders + comandos básicos
  Control_Llanta.ino  → prueba manual de dirección y voltaje
  MPU9250.ino         → verifica sensor IMU (versión original)

================================================================================
26. EVOLUCIÓN DE LA ARQUITECTURA (resumen de cambios desde el inicio)
================================================================================

FASE 1 — Pruebas de hardware (standalone ESP32)
  Contador_Pulsos → Control_Llanta → Pulsos_Max → Tabla_DAC_UP/DOWN
  → MPU9250.ino
  Objetivo: verificar motores, encoders e IMU de forma aislada.

FASE 2 — Control PID básico
  Control_PID_Serial → Control_PID_WIFI → Prueba_HTML_WIFI
  Objetivo: lazo de control cerrado con protocolo Serial y luego WiFi.

FASE 3 — Puente Raspberry Pi
  leds.ino + server.py          (prueba mínima HTTP ↔ Serial)
  → grandroot_bridge.py v1 (Flask, HTTP polling)
  → grandroot_bridge.py v2 (WebSocket asyncio)
  Objetivo: eliminar la dependencia de que el operador esté cerca del ESP32.

FASE 4 — Sensores GPS + IMU
  MPU9250.ino (prueba individual)
  → esp32_sensores.ino v1 (UART → ESP32 PID)
  → esp32_sensores.ino v2 (ESP-NOW, sin cable)
  → ESP32_Sensores_Serial.ino (UART → Raspberry Pi)
  Objetivo: geolocalización de mediciones y telemetría de orientación.

FASE 5 — Dashboard y sistema unificado
  serial_bridge.py + grandroot_dashboard.html  (sensores sólo)
  → Unificacion: Control_PID_WIFI + esp32_sensores (mapa Leaflet embebido)
  → build_dashboard.py (genera index_html.h para PROGMEM)
  Objetivo: sistema 100% offline, sin RPi, con mapa y gráficas en tiempo real.

FASE 6 — Arquitectura RPi centralizada
  app.py (Flask + Socket.IO, dos puertos Serial simultáneos)
  Objetivo: dashboard profesional con reconexión automática y systemd.

ESTADO ACTUAL (junio 2026)
  Dos ramas de producción en paralelo:
  A) Standalone (sin RPi): ESP32 PID (AP + WebSocket) ↔ ESP32 SENSORES (ESP-NOW)
  B) Centralizada (con RPi): app.py ↔ ESP32 PID Serial ↔ ESP32 SENSORES Serial
  Visión autónoma: robot_vision_serial.py + modelo U-Net (placeholder en el dashboard)

================================================================================
  Fin de documentación — GrandRoot_Documentacion_Pruebas
  Generado: 2026-06-05 | Cubre archivos hasta commit: 2026-06-02
================================================================================
